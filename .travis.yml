language: cpp
compiler: g++-4.9
sudo: false

addons:
  apt:
    sources:
      - ubuntu-toolchain-r-test
    packages:
      - g++-4.9

install:
- mv tests/main.cpp.example tests/main.cpp

script: cd tests && make clean debug && ./main
